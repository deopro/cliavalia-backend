/**
 * Custom routes for users-permissions plugin
 * 
 * Note: Business registration route is registered programmatically in strapi-server.ts
 * to ensure the controller is attached before the route is registered.
 */

export default {
  routes: [
    // Business registration route is added programmatically in strapi-server.ts
  ],
};

