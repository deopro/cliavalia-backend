/**
 * Review report routes
 * User-facing: POST /reviews/:id/report and GET /reviews/reports/me are in review API.
 * Admin: GET/PUT review-reports are in admin-dashboard.
 */
export default {
  routes: [],
};
