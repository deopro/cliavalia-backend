/**
 * Review report controller
 * Create/get reports are handled in review controller (POST /reviews/:id/report, GET /reviews/reports/me).
 * Admin list/approve/reject are in admin-dashboard controller.
 */
export default {};
